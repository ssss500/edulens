import 'package:flutter/material.dart';

class FavouriteWindows extends StatelessWidget {
  const FavouriteWindows({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child:Text('fav'));
  }
}
